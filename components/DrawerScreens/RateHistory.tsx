import React from "react";
import { StyleSheet, Text, View } from "react-native";

const RateHistory = () => {
  return (
    <View>
      <Text>RateHistory</Text>
    </View>
  );
};

export default RateHistory;

const styles = StyleSheet.create({});
